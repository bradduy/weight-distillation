{
  "name": "weight-distillation",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "bun build src/cli.ts --outdir=dist --target=bun",
    "typecheck": "bun tsc --noEmit",
    "start": "bun run src/cli.ts",
    "test": "bun test"
  },
  "dependencies": {
    "blessed": "^0.1.81",
    "node-forge": "^1.3.1"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.27",
    "@types/bun": "latest",
    "@types/node": "^20",
    "@types/node-forge": "^1.3.14",
    "typescript": "^5.0.0"
  }
}
